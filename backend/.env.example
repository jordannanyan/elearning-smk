# Salin file ini menjadi .env lalu sesuaikan
PORT=4000

# Koneksi MySQL (default XAMPP: user root tanpa password)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=elearning_smakk

# JWT
JWT_SECRET=ubah-ini-menjadi-string-rahasia-yang-panjang
JWT_EXPIRES_IN=1d

# URL frontend untuk CORS
CLIENT_URL=http://localhost:5173
